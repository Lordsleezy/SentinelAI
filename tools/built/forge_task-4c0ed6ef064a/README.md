# Forge build: task-4c0ed6ef064a

Request:

> You are Forge, SentinelAI's autonomous builder. Build a self-contained tool that fulfills the following request.

User request: create a calculator app and launch it
Target: calculator app
Parameters: {}

Constraints:
- The tool must be runnable from the SentinelAI workspace.
- Prefer pure Python with a clear CLI entry point.
- Write a short README.md alongside the tool.
- Return JSON of {output_path, summary} when finished.

Built via the Ollama fallback (qwen2.5-coder:14b) because node worker exit 1.

Entry point: `forge_task-4c0ed6ef064a.py`
