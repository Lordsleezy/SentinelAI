# ── Core Dependencies ─────────────────────────────────────────────────────────
# Web framework & server
fastapi==0.115.5
uvicorn[standard]==0.32.1
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.4.1

# Scheduling
apscheduler==3.10.4

# HTTP client (Ollama + GitHub API)
httpx==0.27.2
requests==2.32.3

# Browser automation (Algora, IssueHunt)
playwright==1.49.0

# Git operations
gitpython==3.1.43

# Environment / config
python-dotenv==1.0.1

# Data validation (FastAPI dependency)
pydantic==2.10.3

# Web scraping / HTML parsing
beautifulsoup4==4.12.3
lxml==5.3.0

# YAML config parsing
PyYAML==6.0.2

# RSS / news feeds
feedparser==6.0.11

# ── Testing & Quality ──────────────────────────────────────────────────────────
pytest==8.3.4
pytest-timeout==2.3.1
pytest-asyncio==0.24.0

# ── Utilities ──────────────────────────────────────────────────────────────────
pathspec==0.12.1
tenacity==9.0.0
rich==13.9.4
chardet==5.2.0

# ── Orchestration runtime ─────────────────────────────────────────────────────
langgraph>=0.2.60
crewai>=0.86.0
chromadb>=0.5.20

# ── Desktop application ──────────────────────────────────────────────────────
pystray==0.19.5
pillow==10.1.0

# System monitoring (Phase 7)
psutil==5.9.6

# ── Reasoning Quality Enhancements ────────────────────────────────────────────
radon>=6.0.1

# RAG embeddings — required for the orchestration pipeline RAG component
sentence-transformers>=2.7.0
numpy>=1.26.0,<3.0.0

# ── AI providers ──────────────────────────────────────────────────────────────
# Anthropic Claude — confidence escalation
anthropic>=0.39.0
# Ollama Python client (optional helper; httpx is the primary path)
ollama>=0.3.0

# ── Integrations (Pro tier features) ─────────────────────────────────────────
# Telegram / Messaging
python-telegram-bot>=21.0
# Spotify
spotipy>=2.24.0
# Google Calendar
gcsa>=2.3.0
# Camera providers
blinkpy>=0.23.0

xgboost>=2.0.0
websocket-client>=1.6.0
