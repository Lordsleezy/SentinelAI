# Godot installer assets

Place `Godot*_win64.zip` or `Godot*.exe` here for offline Sentinel installs.

Bootstrap copies into `tools/godot/` on first launch.

Stage for release builds:

```powershell
.\scripts\stage_godot_runtime.ps1
```
