"""v2 SKELETON — LSTM sequence model for price prediction.

Requires: tensorflow or pytorch
Status: SKELETON — not active in v1
"""
from __future__ import annotations


class LSTMSignal:
    """v2 LSTM sequence model. NOT active in v1."""

    def predict(self, bars):
        raise NotImplementedError("LSTM not implemented — v2")

    def train(self, data):
        raise NotImplementedError("LSTM not implemented — v2")
